# apps/

Real apps live here, one directory per app. To start a new one, copy a scaffold from `templates/` (`python-app/` or `go-app/`) into `apps/<name>/` and rename the module/package.

This directory is empty in the template itself — `templates/*/` double as the CI smoke-test target until a real app exists here.
